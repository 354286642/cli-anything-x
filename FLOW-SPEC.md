# 流程 Skill 编写规范（Flow Skill SPEC）

流程 Skill 是「业务剧本」：为达成某个业务目标，按什么顺序、带什么判断、调用哪些原子命令。
它建立在原子 Skill 之上，不重复实现接口。（本文「原子命令」泛指原子 Skill 提供的接口调用，含专属命令与接口清单式 request 调用两种形态。）目录与命名约定见根目录 `DIRECTORY.md`。

参考样板：`skills/demo/flows/create-sample-requirement/`。

## 1. 何时写流程 Skill

满足任一即可考虑：

- 一个业务动作需要**串联 >=2 个原子命令**（如「查库存 -> 选商品 -> 建样品」）。
- 存在**表单式交互**：字段有默认值、条件联动（选 A 才出现 B）、必填校验。
- 有明确的**业务目标与成功标准**，而非单纯「调某个接口」。

单接口、无交互逻辑的，仍用原子 Skill。

## 2. 目录与文件结构

```
skills/{project}/flows/{business}/
├── SKILL.md            # 执行主线（必读），建议 <= ~200 行
└── reference/          # 流程独有资料（按需加载）
    ├── fields.md       # 字段字典（全字段、类型、枚举、业务含义）
    ├── examples.md     # 话术示例集（多场景）
    └── verify.md       # 验证脚本（test 环境端到端）
```

复用的枚举/字段词典放 `skills/{project}/_shared/`（见 DIRECTORY §2.4）。

## 3. frontmatter schema

```yaml
---
name: flow-{project}-{business}     # 逻辑 id，必填，如 flow-demo-create-sample
description: >
  业务流程：一句话描述要达成的业务目标（不是「调 /api/x」）。
type: flow                           # 必填，标识流程 Skill
triggers:                            # 触发词，必填
  - 创建样品
  - 样品申请
source_refs:                         # 可选，溯源（保鲜/漂移检测用）
  - demo-service: .../SampleOrderController.java#create
  - feishu: <doc token>
---
```

## 4. SKILL.md 内容模型（固定章节）

按以下章节组织，缺哪项就补哪项；章节名可微调但语义要齐：

1. **业务目标** — 要达成什么（业务语言，非接口语言）。
2. **适用场景** — 什么时候触发本流程。
3. **前置条件** — 已登录、项目已配置、依赖数据已存在等。
4. **流程总览** — 有序步骤的概要（编号），每步标注调用哪个原子接口（专属命令或 request 调用）。
5. **字段依赖图** — 字段间条件联动（如「样品所在地=仓库 -> 出现仓库类型/仓库编码」）。
6. **快速话术模板** — 见 §5，本规范核心。
7. **Agent 引导策略：智能预填 + 确认修改** — 预填规则 + 引导流程，见 §6。
8. **提交命令** — 最终落地的原子命令与请求体结构（含批量）。
9. **辅助接口** — 流程中用到的查询类命令（取默认值、校验等）。
10. **错误处理** — 错误码 -> 恢复动作（如 `AUTH_EXPIRED -> anycli auth login`）。
11. **成功标准** — 如何判定流程成功。
12. **领域知识** — 字段词典摘要、边界、坑（详情下沉 `reference/fields.md`）。
13. **参考文件** — 列出 `reference/*` 与 `_shared/*` 的相对链接。

## 5. 话术模板编写规范（核心）

流程 Skill 的价值在于：用户用一句自然语言「话术」即可触发完整流程，Agent 解析后预填表单、追问缺失项、批量提交。

### 5.1 标准话术格式

提供「可预填 + 用户可改」的模板，把**业务目标 + 关键实体 + 可变参数**写进一句话：

> 给订单{订单名}寄{品牌}{商品名}，条码 {条码}，{数量} 个，从{仓库类型}直接寄出，用于{领用用途}。地址是{收货地址}。快递{邮寄方式}，样品{是否退回}。

示例：

> 给订单沐沐子寄伊菲丹面膜，条码 4560358166538，2 个，从大贸仓直接寄出，用于订单种草。地址是湖北省武汉市武昌区xxx。快递顺丰电商标快，样品需要退回。

### 5.2 预填 vs 必填追问的边界

- **可预填（有合理默认）**：品牌（取用户有权限的第一个）、样品所在地（默认仓库）、样品类型（默认仓库直接寄出）、领用用途、邮寄方式、是否确认（默认「保存并确认需求」）。预填后须向用户**回显并允许修改**。
- **必须追问（无默认、不可编造）**：订单、商品（条码/名称 + 数量 + 效期）、收货地址/收货人。话术里缺失时，**引导用户补充**。
- **严禁**出现「随机生成」「测试数据」「随便填」等措辞或行为。必填字段缺失时只能追问，不能臆造。

### 5.3 批量场景话术

当「其他信息相同、仅订单/地址不同」时，用明细列表一次提交（接口 `detailsList` 支持多元素）：

> 寄{品牌}{商品名}，条码 {条码}，{数量} 个，用于{用途}，从{仓库类型}直接寄出。
> 明细：
> 1. {平台}订单{订单名}，地址：{收货人}{电话}{地址}
> 2. {平台}订单{订单名}，地址：{收货人}{电话}{地址}

Agent 解析每条明细为一个 `detailsList` 元素，**一次 create 批量提交**，而非多次调用。
商品不同（不同条码/仓库/数量/效期）时，需分别确认每个商品明细。

### 5.4 解析规则要点

在 SKILL.md 写清 Agent 如何从话术抽取字段：实体识别（订单/品牌/商品）、单位与数量、仓库类型映射（大贸仓 -> `DM_WAREHOUSE`/`C100`）、地址结构化（收货人/电话/省市区/详细地址）。无法确定的字段进入追问。

## 6. 预填策略写法

用表格固定「字段 -> 默认值来源 -> 是否可改」：

| 字段 | 默认值 | 来源 | 可改 |
|------|--------|------|------|
| brand | 用户有权限的第一个品牌 | `permission/listAllBrandByUser` | 是 |
| sampleLocation | WAREHOUSE | 固定 | 是 |
| sampleType | WAREHOUSE_SEND | 固定 | 是 |
| isConfirm | "1"（保存并确认需求） | 固定 | 是 |

并写清「引导流程」：解析话术 -> 回显预填 -> 追问缺失必填 -> 用户确认 -> 提交（草稿 or 确认）。

## 7. 接地（Grounding）要求

- 流程每一步必须映射到**真实存在**的原子接口（专属命令，或接口清单中的 request 调用）；对不上的先 `anycli gen` 补命令，**禁止幻觉出无法执行的命令**。
- 提交请求体的字段结构须与后端 DTO 一致（字段名、类型、枚举值），并在 `reference/fields.md` 标注来源（Java 类/行号）。
- 条件联动、校验规则以**代码为准**（如 `SendSampleService` 的 `warehouseType != null` 校验、`isConfirm="1"` 才跑 `savaCommCheck`）；业务语义以飞书文档为准；不一致时在 skill 内标注来源与时间。

## 8. 拆分与体量控制

- `SKILL.md` <= ~200 行；超出把字段字典/示例集/验证脚本下沉 `reference/`。
- 枚举/字典被 >=2 处复用 -> `_shared/`；仅本流程 -> `reference/`。
- `SKILL.md` 末尾「参考文件」列出所有下沉文件的相对链接，便于 Agent 按需加载。

## 9. 新建流程 Skill 检查清单

- [ ] 目录 `skills/{project}/flows/{business}/`，`SKILL.md` frontmatter 含 `name: flow-...` + `type: flow` + triggers
- [ ] §4 的章节齐全（至少：业务目标/话术模板/预填策略/有序步骤/字段依赖/错误处理/成功标准）
- [ ] 话术模板符合 §5（含批量），无「随机生成」类措辞，必填缺失走追问
- [ ] 每步接地到真实命令，请求体字段与 DTO 一致
- [ ] 体量大的内容已下沉 `reference/`，复用字典进 `_shared/`
- [ ] 路由表（`skills/anycli/SKILL.md`）已手工登记
- [ ] `reference/verify.md` 提供 test 环境端到端验证脚本并跑通
- [ ] `anycli skill install --force && anycli skill docs` 后归入正确项目组
